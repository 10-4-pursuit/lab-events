function makeBoard() {
    const board = document.createElement("div");
    board.classList.add("board");
// The code below will create a row.
    for (let i = 0; i < 3; i++) {
        const row = document.createElement("div");
        row.classList.add("row");

        for (let s = 0; s < 3; s++) {
            const squareElement = document.createElement("div");
            squareElement
        }
    }
}